import Image from "next/image";

export default function GameCard() {
    return (
        <article className="h-[50px] w-[25px] rounded-sm">
            {/* <Image src={game.cover} alt={game.name} /> */}
        </article>
    )
}